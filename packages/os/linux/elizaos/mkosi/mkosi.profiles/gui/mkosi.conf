# Compatibility alias for callers that still pass --profile=gui. GNOME is the
# canonical desktop in the base image on every architecture; this profile does
# not add Cage, a kiosk compositor, or an architecture-specific fallback UI.

[Output]
ImageId=elizaos-linux-%a-gui
