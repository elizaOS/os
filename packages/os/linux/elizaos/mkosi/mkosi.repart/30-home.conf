[Partition]
# Last partition by design: systemd-repart grows it to fill USB media or the
# selected internal-disk installation target on first boot.
Type=home
Label=elizaos-home
Format=ext4
CopyFiles=/home:/
SizeMinBytes=2G
GrowFileSystem=yes
