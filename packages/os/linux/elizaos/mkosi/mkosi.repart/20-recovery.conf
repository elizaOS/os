[Partition]
# A generic Linux partition contains the factory system tree. The GRUB entry
# installed by mkosi.extra boots it into rescue.target with agent services off.
Type=linux-generic
Label=elizaos-recovery
Format=ext4
CopyFiles=/
Minimize=guess
SizeMinBytes=8G
SizeMaxBytes=8G
