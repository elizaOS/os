# elizaOS Linux desktop session. Electrobun's GTK wrapper currently requires
# X11, so keep GDM on Xorg until its native Wayland path is verified.
[daemon]
WaylandEnable=false
AutomaticLoginEnable=true
AutomaticLogin=user
